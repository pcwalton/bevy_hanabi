This example is more of a debug and stress test.

It shows a grid of effect instances from 2 different effect assets. You can spawn new effect instances with the SPACE key, and delete random existing ones with the DELETE or BACKSPACE key. This example tests batching. It's also useful to test the behavior on effect spawning and despawning.